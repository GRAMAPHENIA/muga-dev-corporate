---
const values = [
  [
    {
      ref: '01',
      name: 'Atención al detalle',
      description:
        'Nos destacamos por cuidar cada aspecto de la estadía de nuestros huéspedes.',
    },
    {
      ref: '02',
      name: 'Sostenibilidad',
      description:
        'Compromiso con el entorno natural y la preservación de la Patagonia.',
    },
  ],
  [
    {
      ref: '03',
      name: 'Cultura local',
      description:
        'Conexión genuina con la cultura y tradiciones de la región.',
    },
    {
      ref: '04',
      name: 'Visión',
      description:
        'Ser reconocidos como líderes en hospitalidad en la Patagonia, destacándonos por nuestra atención al detalle, compromiso con la sostenibilidad y conexión genuina con la cultura local.',
    },
  ],
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col gap-8 lg:gap-12">
  <h2 class="text-3xl font-medium tracking-tight sm:text-4xl font-heading">Valores</h2>
      <div class="grid gap-8 lg:grid-cols-2">
        {
          values.map((column, index) => (
            <div class:list={['space-y-8', index === 1 && 'lg:mt-16']}>
              {column.map((value) => (
                <div class="bg-primary-500/10 flex flex-col gap-12 rounded-3xl px-6 py-8">
                  <p class="text-sm">{value.ref}</p>
                  <div class="flex flex-col gap-4">
                    <h3 class="text-xl font-medium">{value.name}</h3>
                    <p class="text-primary-950/70 text-base sm:text-lg">
                      {value.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
