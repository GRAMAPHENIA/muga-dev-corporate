<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="grid gap-12 lg:grid-cols-2 lg:gap-8">
      <div class="flex flex-col gap-4 sm:gap-6">
        <h2 class="text-3xl font-medium tracking-tight sm:text-4xl font-heading">Consultá disponibilidad o reservá tu cabaña</h2>
        <p class="text-primary-950/70 max-w-lg text-lg sm:text-xl">
          Completá el formulario para recibir información personalizada sobre nuestras cabañas, tarifas y actividades en la Patagonia.
        </p>
      </div>
      <form action="#" method="POST" class="mt-3 flex flex-col gap-y-6">
        <!-- Nombre completo -->
        <div>
          <label for="full-name" class="sr-only">Nombre completo</label>
          <input
            type="text"
            name="full-name"
            id="full-name"
            autocomplete="name"
            class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 placeholder:text-primary-950/60 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base ring-1 transition focus:outline-none focus:ring-2"
            placeholder="Nombre completo"
          />
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="sr-only">Correo electrónico</label>
          <input
            type="email"
            name="email"
            id="email"
            autocomplete="email"
            class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 placeholder:text-primary-950/60 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base ring-1 transition focus:outline-none focus:ring-2"
            placeholder="Correo electrónico"
          />
        </div>

        <!-- Selección de cabaña -->
        <div>
          <label for="cabin" class="sr-only">Cabaña</label>
          <select
            name="cabin"
            id="cabin"
            class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base text-primary-950/80 ring-1 transition focus:outline-none focus:ring-2"
          >
            <option value="">Seleccioná una cabaña</option>
            <option value="lago">Cabaña Lago</option>
            <option value="montana">Cabaña Montaña</option>
            <option value="premium">Cabaña Premium</option>
          </select>
        </div>

        <!-- Fechas -->
        <div class="flex gap-4">
          <div class="flex-1">
            <label for="date-in" class="sr-only">Fecha de ingreso</label>
            <input
              type="date"
              name="date-in"
              id="date-in"
              class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base text-primary-950/80 ring-1 transition focus:outline-none focus:ring-2"
              placeholder="Fecha de ingreso"
            />
          </div>
          <div class="flex-1">
            <label for="date-out" class="sr-only">Fecha de egreso</label>
            <input
              type="date"
              name="date-out"
              id="date-out"
              class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base text-primary-950/80 ring-1 transition focus:outline-none focus:ring-2"
              placeholder="Fecha de egreso"
            />
          </div>
        </div>

        <!-- Mensaje -->
        <div>
          <label for="message" class="sr-only">Mensaje</label>
          <textarea
            name="message"
            id="message"
            rows="3"
            class="hover:ring-primary-600 focus:ring-primary-600 bg-primary-50 ring-primary-900/40 placeholder:text-primary-950/60 block w-full appearance-none rounded-md border-0 px-4 py-4 text-base ring-1 transition focus:outline-none focus:ring-2"
            placeholder="¿Tenés alguna consulta o requerimiento especial? Escribilo aquí."></textarea>
        </div>

        <div>
          <button
            class="bg-primary-600 hover:bg-primary-700 focus-visible:outline-primary-600 inline-flex items-center justify-center rounded-full border border-transparent px-5 py-3 text-base font-medium text-white transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2"
            type="submit"
          >
            Reservar / Consultar
          </button>
        </div>
      </form>
    </div>
  </div>
</section>
